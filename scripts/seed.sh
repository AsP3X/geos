#!/usr/bin/env bash
# Seed the database with sample events and offline reference datasets.
#
# SCAFFOLD STUB: seeding depends on the schema + connectors and is implemented
# with the workers/scripts-polish work.
set -euo pipefail
cd "$(dirname "$0")/.."

echo "seed: not yet implemented — added once the schema and connectors land." >&2
exit 1
