// Runtime API URL for Docker/production (overwritten on container start).
// Local dev: leave empty for same-origin / Vite proxy; or set in .env via
// VITE_API_BASE_URL at build time instead.
window.__GEOS_CONFIG__ = window.__GEOS_CONFIG__ || { apiBaseUrl: "" };
